# Failed Basic Auth attempts against /dashboard/ in Nginx error logs.

[Definition]
failregex = ^.*(?:no user/password was provided for basic authentication|user ".*": password mismatch), client: <HOST>, server: .* request: ".* /dashboard(?:/|\s).*$
ignoreregex =
